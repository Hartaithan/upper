name: Scheduled Up
on:
  schedule:
    - cron: "*/270 * * * *"
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Scheduled Up
        run: curl https://upper-hartaithan.vercel.app/api/up
